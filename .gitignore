node_modules/
src-tauri/target/
dist/
*.log
.DS_Store
.serena/

# Spectra app data
.spectra/

# Auto-dev engineer-loop runtime (loop writes these every round;
# do NOT delete while loop is running, do NOT track in git)
.engineer-loop.pid
.engineer-loop.state.json

# Auto-dev harness sensor reports
.harness-*.json
.harness-*.state

# LP notify daemon runtime (jsonl log + DLQ + health)
.lp-notify*
.lp-notify-cache/

# Auto-dev project lock + Spectra app config
.project.lock
.spectra.yaml

# Auto-dev per-round run records
runs/

# R127 daemon 噪音 (loop/sensor/supervisor/harness-memory 每 round 寫,
# 不在 R13 防護線要追蹤的範圍; .engineer-loop.pid/.state.json + .harness-*.json
# 既有行只覆蓋部分檔, 補 6 個 path 收齊)
.ad-map/
.arch-fitness.json
.engineer-loop.failures.jsonl
.harness-memory.db
.supervisor-report.json
bash.exe.stackdump
